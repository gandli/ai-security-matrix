# SpecterOps/skills

> LLM 技能的市场。

| | |
|---|---|
| **类别** | `skill` |
| **Stars** | 631 |
| **最近更新** | 15d |
| **许可证** | Apache-2.0 |
| **维护者** | 7 |
| **规模** | 15,327 lines · 94 |
| **源码** | <https://github.com/SpecterOps/skills> |
| **作用范围/特征** | `ad`（Active Directory 域） / `binary`（二进制/固件） / `code`（源代码） / `entra`（Entra ID 云身份） / `network`（主机/端口/流量） / `recon`（信息收集） / `redteam`（攻击模拟） / `social`（社会工程） / `webapp`（浏览器应用） / *root* / *credentials* |

## 运行前须知

| 检查项 | 结果 |
|---|---|
| 是否包含未自行构建的预编译二进制 | **yes** |
| 是否要求 root / sudo | **yes** |
| 是否读取 `~/.aws`、`~/.ssh` 等凭据路径 | **yes** |
| 是否在主机上安装软件 | **—** |
| 是否调用项目不拥有的外部端点 | **no** |
| 是否包含无法阅读的超长编码数据 | **no** |

---

_提取自 [aisecuritymatrix.com](https://aisecuritymatrix.com) · [原始页面](https://aisecuritymatrix.com/tools/specterops-skills.html)_