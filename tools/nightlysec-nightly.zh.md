# Nightlysec/nightly

> 自主 AI 渗透测试智能体。

| | |
|---|---|
| **类别** | `agent` |
| **Stars** | 1 |
| **最近更新** | 35d |
| **许可证** | Apache-2.0 |
| **维护者** | 68 |
| **规模** | 59,092 lines · 275 |
| **源码** | <https://github.com/Nightlysec/nightly> |
| **作用范围/特征** | `webapp`（浏览器应用） / `api`（HTTP/RPC 接口） / `code`（源代码） / *root* / *credentials* |

## 内置工具

`arjun`, `dirsearch`, `ffuf`, `httpx`, `katana`, `naabu`, `ncat`, `nmap`, `nuclei`, `semgrep`, `sqlmap`, `subfinder`, `wafw00f`

## 与以下项目共享文件

usestrix/strix

## 运行前须知

| 检查项 | 结果 |
|---|---|
| 是否包含未自行构建的预编译二进制 | **no** |
| 是否要求 root / sudo | **yes** |
| 是否读取 `~/.aws`、`~/.ssh` 等凭据路径 | **yes** |
| 是否在主机上安装软件 | **—** |
| 是否调用项目不拥有的外部端点 | **yes** |
| 是否包含无法阅读的超长编码数据 | **no** |

---

_提取自 [aisecuritymatrix.com](https://aisecuritymatrix.com) · [原始页面](https://aisecuritymatrix.com/tools/nightlysec-nightly.html)_