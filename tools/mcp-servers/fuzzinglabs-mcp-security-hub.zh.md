# FuzzingLabs/mcp-security-hub

> 不断增长的 MCP 服务器集合，为 AI 助手带来进攻性安全工具。Nmap、Ghidra、Nuclei、SQLMap、Hashcat 等。

| | |
|---|---|
| **类别** | `mcp` |
| **Stars** | 792 |
| **最近更新** | 165d |
| **许可证** | MIT |
| **维护者** | 8 |
| **规模** | 13,500 lines · 32 |
| **源码** | <https://github.com/FuzzingLabs/mcp-security-hub> |
| **作用范围/特征** | `network`（主机/端口/流量） / `webapp`（浏览器应用） / `binary`（二进制/固件） / *root* / *credentials* |

## 内置工具

`binwalk`, `hashcat`, `httpx`, `katana`, `masscan`, `medusa`, `naabu`, `nmap`, `nuclei`, `subfinder`, `tlsx`, `waybackurls`

## 运行前须知

| 检查项 | 结果 |
|---|---|
| 是否包含未自行构建的预编译二进制 | **no** |
| 是否要求 root / sudo | **yes** |
| 是否读取 `~/.aws`、`~/.ssh` 等凭据路径 | **yes** |
| 是否在主机上安装软件 | **—** |
| 是否调用项目不拥有的外部端点 | **no** |
| 是否包含无法阅读的超长编码数据 | **no** |

---

_提取自 [aisecuritymatrix.com](https://aisecuritymatrix.com) · [原始页面](https://aisecuritymatrix.com/tools/fuzzinglabs-mcp-security-hub.html)_